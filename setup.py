from setuptools import setup, find_packages

setup (
    name             = "LeadList",
    version          = "0.1",
    description      = "LeadLeast API",
    packages         = find_packages(),
    #install_requires = [],
    #entry_points     = {'console_scripts':
    #                    ['run-the-app = deployme:main']}
)
